# GastOn Backend Environment Configuration Template
# Copy this file to .env and update the values for your environment

# Database Connection (Required)
DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require&channel_binding=require

# Server Configuration
# Note: Render uses PORT=10000 by default, but environment.js handles this automatically
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# Security (for future authentication features)
JWT_SECRET=your_secure_jwt_secret_here

# Database Pool Configuration (Optional)
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_IDLE_TIMEOUT=30000
DB_POOL_CONNECTION_TIMEOUT=2000

# Render Production Example:
# DATABASE_URL=postgresql://neondb_owner:password@host.neon.tech/neondb?sslmode=require&channel_binding=require
# NODE_ENV=production
# CORS_ORIGIN=*
# JWT_SECRET=gaston_jwt_secret_2025_secure_render

# Alternative: Use Render's managed PostgreSQL (free tier, 90-day expiration)
# DATABASE_URL=postgresql://gaston_user:password@dpg-xxxxx.oregon-postgres.render.com/gaston_db